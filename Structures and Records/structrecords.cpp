#include <iostream>
#include <string>
#include <vector>
#include <cstring>
#include <fstream>

struct student
{
    string studentName;
    double score;
};
using namespace std;

int main
{
    ifstream studentRec;
    studentRec.open(StudentScore.txt);
    if(studentRec.is_open())
    {
        cout << a << b;
    }

}